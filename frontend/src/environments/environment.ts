/**
 * Production environment configuration.
 *
 * Used when the Angular application is built for production.
 */
export const environment = {
  production: true,
  apiUrl: 'http://localhost:8080/api/v1',
};
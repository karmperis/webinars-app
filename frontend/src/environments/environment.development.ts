/**
 * Development environment configuration.
 *
 * Used when the Angular application runs locally with ng serve.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api/v1',
};